DRAGNET — {{ today }}
{{ total }} new postings.

{% for p in postings %}
{{ loop.index }}. [{{ "%5.1f"|format(p.score) }}] {{ p.title }} — {{ p.company }}
    Source: {{ p.source }}  |  Location: {{ p.location }}
    Signals: {{ p.signals_summary() }}
{% if p.matched_categories %}
    Categories: {{ p.matched_categories | join(", ") }}
{% endif %}
    {{ p.url }}

{% endfor %}

—
Generated by dragnet. Edit config.yaml to tune scoring or filtering.
